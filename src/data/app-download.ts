/**
 * Download link for the Aksum Academy Android app (.apk).
 *
 * After you build the APK with the GitHub Actions workflow (see
 * BUILD_APK.md), upload the file to Lovable Cloud storage and paste
 * the public URL here.
 *
 * Until then, the homepage button will show a friendly "Coming soon"
 * state instead of a broken download.
 */
export const APK_URL: string | null = null;

export const APK_VERSION = "1.0.0";
export const APK_SIZE_MB = 6;
